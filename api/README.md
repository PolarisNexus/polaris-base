# api/ — 跨服务 API 契约

存放平台所有跨服务的接口定义，是各产品/服务仓库的契约来源。

## 目录结构

```
api/
├── proto/          ← gRPC Protobuf 定义（当前）
└── openapi/        ← OpenAPI 聚合规范（未来）
```

## 原则

- **契约先行**：先在此处定义接口，再到各服务仓库实现
- **版本管理**：Proto 文件变更需同步通知所有消费方
- **向后兼容**：字段只增不删，废弃字段用 `reserved` 标记

# polaris-base 跨组件公共变量
# 复制为 .env 覆盖默认值（可选，所有变量已有内置默认值）
COMPOSE_PROJECT_NAME=polaris-base
TZ=Asia/Shanghai

# 默认激活 plane；按需覆盖为 "data" / "platform" / "services"
COMPOSE_PROFILES=data,platform,services

# ---- 跨服务对齐的凭据（dev 默认；生产改为 Secret）----
# APISIX Admin API Key（components/apisix/config.yaml 内置同值 fallback）
APISIX_ADMIN_KEY=edd1c9f034335f136f87ad84b625c8f1
# Elasticsearch basic auth（platform-admin 的 WAF 攻击日志 + APISIX elasticsearch-logger 都要用）
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=E&9xV2#pM7@zQ4!y
# CrowdSec machine 凭据（platform-admin 走 /v1/watchers/login 拿 JWT 读 alerts / 写 decision）
CROWDSEC_AGENT_USERNAME=platform-admin
CROWDSEC_AGENT_PASSWORD=polaris-dev-agent-password
# CrowdSec bouncer 凭据（platform-admin 作为 bouncer 读 /v1/decisions；LAPI 用 X-Api-Key 区分 machine/bouncer）
CROWDSEC_PLATFORM_ADMIN_BOUNCER_KEY=polaris-dev-bouncer-platform-admin
